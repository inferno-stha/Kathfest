# ============================================================
# requirements.txt - Python dependencies for the Smart Student
# Attendance System Using Face Recognition.
#
# Installation:
#     pip install -r requirements.txt
#
# Notes:
#     - numpy and opencv-python use loose version pins (>=) to
#       ensure pre-built wheels are available for your platform.
#     - face_recognition requires dlib, which needs C++ build
#       tools. If installation fails, the app still works for
#       all non-camera features.
# ============================================================

Flask>=3.0.0
opencv-python>=4.9.0
face_recognition>=1.3.0
numpy>=1.24.0
